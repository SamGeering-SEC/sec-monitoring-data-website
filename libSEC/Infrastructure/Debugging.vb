'Imports System.Web.Mvc

Public Class Debugging

    'Public Shared Sub LogModelStateErrors(ModelStateDictionary As ModelStateDictionary)

    '    Dim errors = From modelstate In ModelStateDictionary.AsQueryable().Where(Function(f) f.Value.Errors.Count > 0) Select New With {.Title = modelstate.Key}
    '    For Each e In errors
    '        Debug.WriteLine(e.ToString)
    '    Next

    'End Sub

End Class
